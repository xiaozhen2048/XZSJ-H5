// js/config.js — Global configuration constants
window.APP_CONFIG = {
  // Backend API base URL. Change to Railway URL for production.
  API_BASE: '',  // Pages Functions on same domain via custom domain

  // Activation code prefix for branding
  CODE_PREFIX: 'XZST',

  // localStorage key names
  STORAGE_KEYS: {
    TOKEN: 'xzst_token',
    ACTIVATION: 'xzst_activation',
    THEME: 'xzst_theme',
  }
};
